IO.puts "# Go into `iex`; create and run the functions that do that following:"
IO.puts "list_concat.([:a, :b], [:c, :d]) # => [:a, :b, :c, :d]"
IO.puts "sum.(1, 2, 3) # => 6"
IO.puts "pair_tuple_to_list.( { 1234, 5678 } ) # => [ 1234, 5678 ]"
